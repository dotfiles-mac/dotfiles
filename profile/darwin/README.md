# Darwin profile

This directory contains a lightweight macOS (Darwin) bootstrap script.

File: profile/darwin/install.sh

Usage: bash profile/darwin/install.sh [--quiet|--verbose|--help]

Behavior:
- Exits silently on non-macOS hosts.
- Quiet by default; pass --verbose to see actions.
